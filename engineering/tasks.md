# Tasks

## Decisions needed from you

This section holds only open items — a topic that is absent is settled and recorded in `engineering/decisions.md`, not forgotten. **This list contains only open items; an absent topic is settled, not forgotten.**

**No open decisions.** The grouping below is feature-sliced (each task crosses the modules its feature touches, per the outside-in rule) with a walking skeleton first; ordering follows dependency, not module boundaries.

Changes since last approval: none — this version was approved on 2026-08-08.

Every task is one branch delivering one reviewable change, started with its failing end-to-end test per the testing rules where the task carries a user-visible surface; the two disclosed infrastructure tasks (T2.1, T5.1) start from their failing integration-level contract tests instead, their end-to-end coverage arriving with the surface task that consumes them (T2.2, T5.2). A branch merges only with its stated tests passing and the full suite green at the branch tip. Checkboxes are ticked by the main session only, immediately on verified completion.

## T1 — Walking skeleton

- [ ] T1.1 Repository scaffolding and the thinnest end-to-end path: Bazel workspace; a Rust binary crate per module (`server`, `sessions`, `agent`, `mcp`, `printer`, `fileindex` as library crates behind one `filamentary` binary) wired at the composition root with essentially no logic; the TS/React app shell built and embedded; the service starts, binds the configured port, prints one URL + QR per address, and serves the UI shell with the persistent header carrying the LOCAL/EXPOSED badge (R1) on every screen; a minimal configuration pass-through (env/flag reads for the v1 options, enough for tests to point a spawned binary at a port and a printer document — the full precedence chain, TOML file, and config surface arrive in T9). One end-to-end test (Playwright against the spawned binary) drives exactly that and goes green. Creates `.claude/test-commands.json` (fast/full/release as Bazel tag filters per the architecture's test strategy) and the repo-local merge gate in `.claude/settings.json` (PreToolUse on `git merge` running the full command).

## T2 — Message lifecycle core

- [ ] T2.1 The MCP endpoint lifecycle: `endpoint_for`/`revoke` per the contract's context rules — the persisted table, tokens, tombstones, existence confirmation, URL composition, serialization (the tool catalog comes later, in T5.2) — plus `sessions.resolve`, the session-store existence query the re-read confirmation consumes, built here with its tests (its session-store siblings arrive in T3). Contract/integration coverage per mcp's test plan item 1; its user-visible path arrives with T2.2's creation E2E, the feature this task exists for.
- [ ] T2.2 Session creation with first message, mocked backend: `create_with_first_message` end to end — validation and the synchronous rejection families, the creation journal, the creation-context `agent.send` against a scripted fake sidecar with its symmetric unwind, name derivation, the session-created and entry events, `message_landed`/`turn_landed` wiring. E2E: creating a session from the browser lands the message with the mocked reply visible on reload; a provisioning fault rejects the creation whole (live streaming arrives with T2.3–T2.4).
- [ ] T2.3 Events transport: SSE stream, tokens, replay ring, resync, heartbeat, fan-out registry, fetch-then-subscribe composition in the sync store. E2E: two browser contexts converge on one session without refresh; reconnect replays exactly the missed events.
- [ ] T2.4 The turn stream: sink relay and commitment — deltas as ephemerals over the T2.3 transport, text-segment close/flush rules, tool call/result entries verbatim, terminals, the turn-in-flight flag and ephemerals, the one-turn lock and busy. E2E: a streamed reply renders live and identically after reload.
- [ ] T2.5 Failure and retry: the classification mapper against recorded-shape fixtures, failure entries with cause/remedy, the pending store's dual retention, retry with its rejections. E2E: a mocked auth failure renders its cause and remedy and retries to success.
- [ ] T2.6 Automatic resume: the resume scheduler — arming with the reset-time parser, at-most-once fire, supersession, re-arm, the arming clause's unarmed shape. E2E: a mocked usage-limit failure renders with reset time and auto-resumes at it.
- [ ] T2.7 Credential determination and recycle: attribution, no-silent-fallback, the content-fingerprint probe, changed-probe recycle severing per the recovery-isolation bound — all against the scripted fake and injected probe (R2.2's mocked coverage; the real spawn mechanics stay in T10.1). E2E: with the fake backend failing auth, rewriting the probed credential material makes the next attempt succeed with no service restart (R2.2's CLI-login criterion, mocked).
- [ ] T2.8 The persistence-failure condition: the in-memory retry queue with aggregation, causality hold, and clearing; session-failure events with cause and key-set; the fetchable condition; deletion supersession; the ui stuck-content banner. Coverage at the levels the module docs assign — the injected-store-fault mechanics at unit/integration level (sessions test 7, events S5) and the banner against fixture events (ui test 10) — with no end-to-end fault hook, per the injected-fault rule.
- [ ] T2.9 Crash recovery: the startup scan machinery in contract order over the steps that exist now — pending restore and drain, creation-journal residue resolution, turn recovery with `turn_landed` re-synthesis — with the later steps joining the scan as their groups land (T3.3's cascade re-drive, T7.1's re-derivation pass), each tested by its own task. E2E: kill mid-turn, restart, the interrupted failure stands with working retry and no duplicates; a graceful restart preserves the session list, transcripts, and evidence, with a reopened session continuable (R4).

## T3 — Session store surface

- [ ] T3.1 Listing, search, rename, archive: the session-store contract's `list`/`search` with the show-archived selector and paging, FTS, rename, archive/unarchive, their events, and the ui screens. E2E: R4's criteria — search by name and by transcript content, archive round trip, reopen-while-archived.
- [ ] T3.2 Transcript and state fetch: paged transcript, `session_state` with conditions, reopen with full history. E2E: a long transcript pages gaplessly against live commits.
- [ ] T3.3 Deletion: the confirmed-deletion flow, tombstone-first observability, the journaled cascade machinery with re-drive over the steps that exist now (tombstone, `agent.teardown`, row removal), the fan-out drop rule — the evidence-file and `fileindex.remove_session` steps join the cascade with T4.2, tested there with real evidence. E2E: deletion is immediate on every device and survives a mid-cascade restart.

## T4 — Evidence

- [ ] T4.1 Image uploads and the gallery: intake pipeline (validation, EXIF, derivatives), evidence rows and events, upload routes with kind declaration, gallery pagination and upload progress. E2E: phone-style capture upload lands in the gallery on every device; each rejection reason renders distinctly.
- [ ] T4.2 File uploads and evidence reads: `.3mf`/gcode intake via `fileindex.validate_and_index`, the evidence read routes (Display/Original), reproduction-from-original, the error partition, and the deletion cascade's evidence steps joining T3.3's machinery (file removal, `fileindex.remove_session`). E2E: project upload accepted, invalid file's reason relayed, download serves original bytes; deleting a session with uploads removes its files, index, and every access path, surviving a mid-cascade restart (R4 + the evidence revocation guarantee).
- [ ] T4.3 Prompt assembly with refs: send-site rejections (unknown-ref, unservable, cap, empty), image refs as labelled Display content, file refs as handles, the domain-instruction assets — the finding/evidence/change structures, ask-for-the-`.3mf`, the evidence-quality pattern, and R8's calibration guidance (OrcaSlicer test naming and the feed-back flow), asserted on the composed instructions per sessions test 10a — and markup parsing into the two typed kinds. E2E: a mocked reply carrying finding and evidence-request markup renders the branded registers and request card, and a scripted calibration reply renders its exact test name and settings verbatim (fixture-derived per the mocked-suite rule; R8's live verification rides T10.2).

## T5 — File index and MCP tools

- [ ] T5.1 fileindex views: settings (lookup/section/full/diff), gcode windows/layer/feature/search, mesh measurements and rendered views, thumbnails, summaries, `printer_name` — bounds, pinning, continuations, no-upload answers, using the `testdata/` fixture pair. Unit/integration level per its test plan (agent-facing E2E arrives with T5.2).
- [ ] T5.2 MCP catalog and per-call pipeline: the pipeline's fixed order over the endpoint lifecycle T2.1 built, the catalog as data with continuation/kind/ref addressing rules, verbatim relay, and the tool families buildable now (the `fileindex` views, evidence list and image read). The catalog grows with each later feature group — its printer-facing tools land with T8, each group's task carrying its tools' round-trip assertions — and the catalog-completeness assertion (every declared tool resolves, every schema round-trips, enumerated over the catalog as data) lands with T8.3 once the catalog is whole. E2E: the mocked agent's calls round-trip the families built so far with distinct errors; a revoked token is rejected outright.

## T6 — Printer knowledge, session-independent

- [ ] T6.1 Document interpretation: `set_document_location` (wiring and live), the loader/hash retention, interpretation via `agent.interpret_document` against the recorded reply shape, the freshness machine with all six causes, retrigger classes, the printers screen's list + freshness rendering. E2E: R3's flow — document set, list appears; edit, list follows; each failure cause renders with path and problem.
- [ ] T6.2 Reachability and telemetry: the allowlisted Moonraker client against scripted fixtures, reachability verdicts and events, telemetry with provenance and staleness judgment, and their session-independent printers-screen routes and rendering. E2E: R6's telemetry criteria on the printers screen.
- [ ] T6.3 Klipper log and config views: log tail/range/search with the restart boundary and rotation refusal, config section list/reads with the changed-content refusal, their session-independent routes and rendering. E2E: R6's log and config criteria on the printers screen.

## T7 — Binding

- [ ] T7.1 The derivation: the pure function over its full input matrix, the emission comparator, last-announced persistence, auto-bind write, the bind route and control, binding-changed events with trace notes, every ui cell. E2E: R-binding criteria — named upload auto-binds, ambiguity annotates, manual bind sticks, document edits re-derive live. (The gates at consumers are built with the consumers, in T8.)

## T8 — Session-scoped printer access (built on the T7.1 gate)

- [ ] T8.1 Gated agent tools and the session view's printer state: `mcp`'s printer-facing tools (telemetry, log, config) joining the catalog with the binding-gate column composed into their answers, unknown-printer remedies via the re-read at `server`, and the session view's degraded bar and no-telemetry notice. E2E: an unbound session's agent is told to ask; the gated tools answer their states distinctly; R6's session-view criteria.
- [ ] T8.2 Discovery and the camera choice: `webcams` with offering scope, the binding-gated listing route, the picker, choice set/read with scope pass-through, camera-choice events. E2E: R7's discovery and choice criteria across devices.
- [ ] T8.3 Capture both paths: user snapshots (server route, provenance intact) and agent snapshots (the mcp snapshot tool completing the catalog, gating reasons, the three camera shapes), the full refusal partition, capture-failed vs fetch-failure, and the catalog-completeness assertion over the finished catalog. E2E: R7's capture criteria with every refusal rendered distinctly.

## T9 — Configuration surface and startup polish

- [ ] T9.1 Configuration and startup: TOML/env/flag precedence over the v1 option set, the absent-file first run, the fatal startup causes each with its distinct printed cause, the log sink with stderr fallback. E2E: R1's start-and-connect criteria including per-address QR and the badge's LOCAL/EXPOSED readings; the occupied-port exit, spawning the real binary against a taken port and asserting the non-zero exit code and printed cause (the architecture's binary-spawn carve-out); the service log exists at the documented path after a run and contains the requests served (R10).
- [ ] T9.2 The config surface and the location set route: the surface's state rendering and help text, the set route with shadowing rejection and persist-then-hand-over semantics. E2E: the location-unset remedy works live from the surface, and a flag-shadowed set is refused naming the source.

## T10 — Real backend and release readiness

- [ ] T10.1 Sidecar integration: vendor the pinned `claude-agent-acp` + Node runtime, real spawn/recycle mechanics, credential probe with content fingerprint, single-flight spawn, silence-not-progress bounds — all still under the fast/full suites via the scripted fake, with the live path behind the release tag (the credential-determination and recycle *logic* landed in T2.5; this task makes it real). E2E: T2.5's R2.2 scenarios re-run against the real sidecar manager with the fake transport, the live-CLI variant riding T10.2's release items.
- [ ] T10.2 The ACP release items as `live`-tagged tests: real error shapes for auth/usage-limit/outage, the update-kind vocabulary, structured tool results, `session/load` across processes, concurrent prompting, streamable-HTTP adapter acceptance, the `~/.claude` probe layout, spawn-time credential caching, credential precedence with both sources present and no silent fallback on a rejected token against the live SDK, live interpretation, R9's markup emission, R8's calibration behaviour (a live exchange naming the exact OrcaSlicer test, and the fed-back result producing the concluded value with exact profile settings), and the evidence contract's vision item — an agent-served image confirmed reaching the live model as vision input — recording replaces each provisional fixture on first run. Emptying `agent`'s provisional-mocks section is this task's exit criterion.
- [ ] T10.3 The printer- and evidence-side release items as `live`-tagged tests: real Moonraker responses per allowlisted endpoint, crowsnest/Mainsail snapshot-URL resolution, the klippy.log boundary and rotation behaviour, a real camera body through the intake validator, and a real phone-captured HEIC through intake. Emptying `printer`'s provisional-mocks section is this task's exit criterion — its interpretation-conversation entry is cleared by T10.2's live-interpretation recording, so this task confirms that entry rather than re-recording it.
- [ ] T10.4 README and consumer docs: `README.md` (description, when to use, install, quick start) per the docs phase; brand assets per `brand/design-language.md`.
